#include "miner.hpp"

namespace Board
{
    // Constructor
    Miner::Miner() : Piece() {}
    std::vector<std::pair<int, int>> Miner::getAllMoves(int *pos, int *dim)
    {
        std::vector<std::pair<int, int>> arr;
        return arr;
    }
    std::vector<std::pair<int, int>> Miner::getAllAttacks(int *pos, int *dim)
    {
        std::vector<std::pair<int, int>> arr;
        return arr;
    }
}