#include "sentinel.hpp"

namespace Board
{
    // Constructor
    Sentinel::Sentinel() : Piece() {}
    std::vector<std::pair<int, int>> Sentinel::getAllMoves(int *pos, int *dim)
    {
        std::vector<std::pair<int, int>> arr;
        return arr;
    }
    std::vector<std::pair<int, int>> Sentinel::getAllAttacks(int *pos, int *dim)
    {
        std::vector<std::pair<int, int>> arr;
        return arr;
    }
}